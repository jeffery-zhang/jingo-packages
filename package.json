{
  "name": "pnpm-monorepo-starter",
  "version": "1.0.0",
  "description": "A typescript monorepo project based on pnpm workspace",
  "scripts": {
    "dev": "pnpm run -r dev",
    "build": "pnpm run -r build",
    "build:page": "pnpm run -r build:page",
    "d": "pnpm build:page && pnpm wrangler pages deploy apps/example/dist"
  },
  "keywords": [
    "pnpm",
    "monorepo",
    "starter",
    "boilerplate",
    "vite"
  ],
  "author": "JINGO",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.8.2",
    "wrangler": "^4.46.0"
  }
}
